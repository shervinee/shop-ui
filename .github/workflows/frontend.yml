name: Shop UI

on:
  workflow_dispatch:

  pull_request:
    paths:
      - "shop-ui/**"
  push:
    branches:
      - main
      - develop
      - hotfix-*

jobs:
  build_test_lint:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: shop-ui
    env:
      CI: "true"
